1 TOKEN_DATATYPE TOKEN_ID = EXPRESSION;
1 TOKEN_DATATYPE < TOKEN_INTEGER > TOKEN_ID = EXPRESSION;
2 if ( EXPRESSION ) : $ADD_INDENT(SIZE(EXPRESSION))
3 elif ( EXPRESSION ) : $ADD_INDENT(SIZE(EXPRESSION))
4 else : $ADD_INDENT()

EXPRESSION:
    EXPRESSION * EXPRESSION
    EXPRESSION / EXPRESSION
    EXPRESSION + EXPRESSION
    EXPRESSION - EXPRESSION
    !EXPRESSION
    ~EXPRESSION
    (EXPRESSION)
    FUNCTION(EXPRESSION)
    INPUT(TOKEN_DATATYPE)

FUNCTION:
    "pow"

TOKEN_DATATYPE:
    single:
        if(TOKEN_INTEGER == 1){
            ALLOCATION = "char *"
        }else{
            ALLOCATION = "int "
        }

INPUT:
    single:
        value = "%d"


1 "%s%s = %s" ALLOCATION TOKEN_ID EXPRESSION;
2 "if(%s){}" EXPRESSION
3 "else if(%s){}" EXPRESSION
4 "else"
