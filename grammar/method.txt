//Declaration
TOKEN_DATATYPE TOKEN_ID = EXPRESSION    => declare % %%; %% = %%%;
TOKEN_DATATYPE TOKEN_ID                 => declare % %%; 
//Errors in declaration
TOKEN_DATATYPE TOKEN_ID =               // expecting expression after '='
//Conditional
if ( EXPRESSION ) :                     => comp 0 %; jump if;
else if ( EXPRESSION ) :                => comp 0 %; jump elseif;
else :                                  => jump else;
//Loop
for ( TOKEN_DATATYPE TOKEN_ID from EXPRESSION to EXPRESSION ) : => declare %%;
while ( EXPRESSION ) :                  
print { }                               // No Arguments are given for print
print { ARGUMENT }                       