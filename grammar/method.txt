//Declaration
TOKEN_DATATYPE TOKEN_ID = EXPRESSION $$DECLARE
//Errors in declaration
TOKEN_DATATYPE TOKEN_ID = // expecting expression after '='
//Conditional
if ( EXPRESSION ) :
else if ( EXPRESSION ) :
else :
//Loop
for ( TOKEN_DATATYPE TOKEN_ID from EXPRESSION to EXPRESSION ) :
while ( EXPRESSION ) :
print { } // No Arguments are given 
print { ARGUMENT }