Start an if statement block with condition (42 + 3 + 5)

Declare a variable named "b" with datatype which allocates memory of 4 and saves the address. and assign the value (3)

Declare a variable named "c" with datatype which allocates memory of 4 and saves the address. and assign the value (4)

end of the finally mentioned block

Start a for loop block by iterating a variable nameda with datatype which allocates memory of 4 and saves the address.. Iterate from the value (1) to the value (6).
Declare a variable named "d" with datatype which allocates memory of 4 and saves the address. and assign the value (3)

